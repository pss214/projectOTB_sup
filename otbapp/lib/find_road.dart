import 'package:flutter/material.dart';

class NavigationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('길찾기'),
      ),
      body: Center(
        child: Text('길찾기 페이지입니다 API 가져와서 길찾기 추가해야합니다'),
      ),
    );
  }
}
